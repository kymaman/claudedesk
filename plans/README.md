# Implementation Plans

Generated by the improve skill on 2026-06-11 (advisor: Claude Fable 5, audit at
commit `0d7fc92`). Full vetted findings table: [AUDIT.md](AUDIT.md). Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Selection note: run non-interactively, so per the skill's default the top 5
findings by leverage were planned. Findings 6–12 in AUDIT.md await the owner's
pick for the next round.

## Execution order & status

| Plan | Title                                                        | Priority | Effort | Depends on       | Status |
| ---- | ------------------------------------------------------------ | -------- | ------ | ---------------- | ------ |
| 001  | Cache + parallelize session-lineage scan (Tree tab perf)     | P1       | M      | —                | DONE   |
| 002  | Remote-access hardening: Origin check, sessionStorage, CSP   | P1       | S      | —                | TODO   |
| 003  | Make silent catch blocks observable (chats, session-history) | P2       | S      | —                | TODO   |
| 004  | Run unit tests in CI + raise vitest timeout                  | P2       | S      | —                | TODO   |
| 005  | De-sleep the 4 flakiest e2e specs                            | P3       | M      | better after 004 | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 005 is independent but lands best after 004: with units in CI the owner sees
  green/red signal while e2e is being de-flaked.
- 001 and 003 both touch nothing in common despite both involving
  session-history adjacency — safe in parallel **in separate worktrees**; in
  one working tree do them sequentially (003 edits `session-history.ts`,
  001 explicitly must not).

## Repo ground rules for every executor

- Never `git push`, tag, or release — the owner pushes only on his explicit
  command.
- Never run e2e without `CLAUDEDESK_E2E=1` isolation; never run units and e2e
  (or a vite build) simultaneously — load causes false timeouts.
- Pre-commit hook runs `eslint --max-warnings 0`: no non-null assertions
  (`x!`), write guards.
- After changing `src/`, rebuild `npm run build:frontend`; after `electron/`,
  also `npm run compile` — the owner launches the app from built artifacts.
- Never delete or modify files under `~/.claude/projects` (real user data).

## Findings considered and rejected

See the "Рассмотрено и ОТКЛОНЕНО" section of [AUDIT.md](AUDIT.md) — 10 entries
(false positives from the audit subagents, each refuted against the code), so
nobody re-audits them.
