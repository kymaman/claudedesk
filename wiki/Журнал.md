# Журнал — что сделано, когда и почему

Бэклог проекта, новое сверху. Формат: дата → что просил Игорь → что сделано → коммиты.

## 2026-06-11

**Просил**: установить /improve и прогнать аудит; починить прокрутку мышкой
(«пропускаю строчки»); записать всё в отдельную папочку и в git; вести вики проекта
в Obsidian + протокол ТЗ; убрать вкладку Tree сверху и встроить дерево в History
(«одна входит в другую»); собрать базу его запросов из истории; планы аудита — сначала
Sonnet, не справился → Opus.

**Сделано**:

- Скролл: 5 строк/тик → 3 + плавная анимация 120мс (`0d7fc92`). Причина: при 5 глаз
  терял строчки, при 1 (старое) — «еле двигается».
- /improve установлен (`npx skills add shadcn/improve`, прокси-байпас) — `b352693`.
- Аудит: 4 субагента + ручная верификация → `plans/AUDIT.md` (12 подтверждено,
  10 отклонено) + 5 планов-исполнений `plans/001..005` — `b352693`.
- Вчерашний блок (Tree tab, lineage, строки 2.1, полные ответы) закоммичен — `8a76cfc`.
- Вики `wiki/` создана (Home, Журнал, Грабли, Решения, Задачи/, Запросы-Игоря).
- База запросов Игоря собрана из 11 сессий (~70 запросов) → [[Запросы-Игоря]].
- Дерево перенесено из вкладки в History: ▸ на строках с ветками, граф инлайн
  (`SessionFamilyGraph`), вкладка Tree удалена (`fa5f28a`). e2e
  `history-tree-inline.spec.ts` 2/2 (Sonnet, реальные данные).
  ТЗ: Задачи/2026-06-11-дерево-в-history.
- Планы 001–003 выполнены **Sonnet-агентами**, диффы проверены Claude как тех-лидом,
  все три приняты с первого раза, эскалация к Opus не понадобилась:
  001 кэш+параллельный скан родословной (`6af5dc8`, 11/11 тестов),
  002 защита remote: Origin-check/sessionStorage/CSP (`a6280cf`, 6/6),
  003 логирование в 13 молчаливых catch (`4b33942`, 172 теста, ноль изменений поведения).
  Планы 004 (CI-юниты) и 005 (e2e-флаки) — в очереди (plans/README.md).

### Позже в тот же день (баги + методология)

**Просил**: рассказать про методологию ТЗ «крутых технарей»; протестировать, потянет ли
Sonnet «голую» задачу без моих калек; ▸ плохо видно + дерево «чёрный экран, ничего не
видно» → как в Obsidian/iPhone-заметках отступами; скроллбэк опять пропал в resumed-сессии,
найти причину + тест red→green.

**Сделано**:

- Методология: User Story/BDD/ADR/RFC → [[Методология-задач]]; [[ТЗ-шаблон]] переписан
  (Проблема→Что надо→Критерии→Тесты→Границы).
- Эксперимент: Sonnet БЕЗ плана-кальки сделал защиту mermaid-рендера (DOMPurify) — справился
  сам, 7 тестов, `d27a052`. Вывод: на чёткой задаче Sonnet тянет без пошаговых планов.
- Дерево: тёмный SVG → отступный аутлайн (`outlineFamily`, `SessionFamilyGraph`,
  Obsidian-стиль); ▸ ярче/крупнее. e2e обновлён (`.fam-outline`), Sonnet 2/2.
- Скроллбэк: причина — скан дерева на mount History душил main, IPC транскрипта не успевал
  за 3с-гейт, старое `!prePtyGateOpen` ВЫБРАСЫВАЛО транскрипт. Фикс: `shouldWriteTranscript`
  (пишем всегда, если пришёл и терминал жив) + скан отложен на 1.5с. Тест green→red→green
  показан. Всё в `c8f86b9`. → [[Грабли]].

## 2026-06-10

**Просил**: запушить стабильную версию; починить «бранч не от той сессии»; дерево
сессий с вариантами дизайна (выбрал 1В + строки 2.1, без авто-сводок); найти потерянный
диалог про Wispr Flow после краша; «опять не вижу полные ответы»; тесты в конце — только
по новым функциям.

**Сделано**:

- v0.3.12-stable запушен (`27fef01`, тег, main fast-forward).
- Найден потерянный диалог: `fc4a2f36-010c-416c-8d4a-48c960441aad` (zenbu/Wispr Flow).
- Бранч-баг исправлен (live-watch + мгновенный пересчёт перед форком).
- Модуль родословной `session-lineage.ts` + тесты на РЕАЛЬНОЙ семье Wispr Flow
  (корень `110b89b0` от 28 мая). Попутно вскрыты грабли NTFS/timestamps → [[Грабли]].
- Вкладка Tree (1В) + двухстрочные строки History (2.1) + «⑂ из родителя».
- Полные ответы: рендерер прятал нумерованные списки и сообщения с фразой
  «ran out of context» — обе причины найдены тестами и починены
  (`session-transcript-full-answers.test.ts`).

## 2026-06 (ранее, сжато)

- Префилл истории в xterm (claude не перерисовывает на --resume); удаляли в `6d4b1c8` —
  вернули, не удалять (см. [[Грабли]]).
- AI-заголовки сессий (haiku), bump даты при открытии, e2e-изоляция CLAUDEDESK_E2E=1,
  инлайн-ренеймы вместо window.prompt, фикс двойной вставки Wispr Flow.
- Пин claude 4.7 в отдельный каталог (авто-апгрейд затирал), агент 4.8.
- Полный прогон после фиксов: e2e 84 pass, юниты 442 pass.
