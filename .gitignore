node_modules
dist
dist-electron
dist-remote
release
.worktrees
.idea
.claude
.DS_Store
# docs/ is ignored upstream — we keep the screenshots subfolder for README.
docs/*
!docs/screenshots/
!docs/screenshots/**

# Sandbox bind-mount artifacts from user home (not project files).
# Root-anchored so legitimate nested files with these names are still tracked.
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.mcp.json
/.profile
/.ripgreprc
/.zprofile
/.zshrc
